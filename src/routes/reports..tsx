import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, CalendarDays, Download, FileText, Printer, ShieldAlert } from "lucide-react";
import { Button } from "@/components/ui/button";
import { PageIntro, StatusPill, Surface } from "@/components/veridoc-shell";
import { demoChecks, demoDocument } from "@/lib/veridoc-data";

export const Route = createFileRoute("/reports/")({
  head: () => ({ meta: [{ title: "Verification report — VeriDoc AI" }, { name: "description", content: "View a fictional VeriDoc AI document screening report and recommendation." }, { property: "og:title", content: "Verification report — VeriDoc AI" }, { property: "og:description", content: "View a fictional VeriDoc AI document screening report and recommendation." }] }),
  component: Report,
});

function Report() {
  const download = () => {
    const text = `VERIDOC AI — SAMPLE VERIFICATION REPORT\n\nDocument ID: ${demoDocument.id}\nScreening date: 08 Sep 2026\nRisk score: ${demoDocument.score}/100\nClassification: ${demoDocument.classification}\n\nRecommendation: Continue with manual review.\n\nThis report contains fictional demo data. AI screening provides a risk assessment and does not constitute official identity verification.`;
    const blob = new Blob([text], { type: "text/plain" });
    const url = URL.createObjectURL(blob);
    const anchor = document.createElement("a");
    anchor.href = url;
    anchor.download = `${demoDocument.id}-sample-report.txt`;
    anchor.click();
    URL.revokeObjectURL(url);
  };
  return <div className="app-grid min-h-[calc(100vh-70px)] px-5 py-8 sm:px-8 lg:px-10"><div className="mx-auto max-w-[1180px]"><PageIntro eyebrow="Verification report · Sample" title="Screening report" description="A printable record of the structured screening result for authorized review." action={<div className="flex gap-2"><Button variant="outline" onClick={() => window.print()}><Printer size={15} />Print</Button><Button onClick={download}><Download size={15} />Download</Button></div>} /><div className="mb-5 flex items-center gap-2 rounded-md border border-warning/25 bg-warning/10 px-4 py-3 text-xs text-warning-foreground"><ShieldAlert size={16} /><strong>FICTIONAL SAMPLE REPORT</strong><span className="text-warning-foreground/70">· Not valid for identity verification or official decision-making.</span></div><Surface className="overflow-hidden print:border-0 print:shadow-none"><div className="flex flex-col justify-between gap-5 border-b border-border bg-navy px-6 py-7 text-primary-foreground sm:flex-row sm:items-end sm:px-8"><div><div className="flex items-center gap-2 text-xs font-semibold"><div className="flex h-8 w-8 items-center justify-center rounded-md bg-sidebar-primary text-sidebar-primary-foreground"><FileText size={17} /></div>VeriDoc <span className="text-cyan">AI</span></div><h2 className="mt-6 text-2xl font-semibold">Document screening report</h2><p className="mt-2 text-xs text-primary-foreground/60">Prepared for authorized reviewer use · Demo Mode</p></div><div className="text-left sm:text-right"><div className="data-mono text-sm font-semibold">{demoDocument.id}</div><div className="mt-2 flex items-center gap-2 text-[11px] text-primary-foreground/60 sm:justify-end"><CalendarDays size={13} />08 Sep 2026 · 10:42 AM</div></div></div><div className="grid gap-px bg-border sm:grid-cols-3"><div className="bg-card px-6 py-5 sm:px-8"><div className="text-[10px] uppercase tracking-[0.12em] text-muted-foreground">Risk score</div><div className="mt-2 data-mono text-3xl font-semibold text-warning-foreground">{demoDocument.score}<span className="text-base text-muted-foreground">/100</span></div></div><div className="bg-card px-6 py-5 sm:px-8"><div className="text-[10px] uppercase tracking-[0.12em] text-muted-foreground">Classification</div><div className="mt-3"><StatusPill value={demoDocument.classification} /></div></div><div className="bg-card px-6 py-5 sm:px-8"><div className="text-[10px] uppercase tracking-[0.12em] text-muted-foreground">Reviewer</div><div className="mt-3 text-sm font-semibold">Ananya Sharma</div></div></div><div className="grid gap-7 px-6 py-7 sm:px-8 lg:grid-cols-[1fr_1fr]"><div><h3 className="text-xs font-bold uppercase tracking-[0.14em] text-cyan">Extracted information</h3><div className="mt-4 grid grid-cols-2 gap-x-5 gap-y-4">{[["Name", demoDocument.name], ["Date of birth", demoDocument.dob], ["Document number", demoDocument.documentNumber], ["Issuing authority", demoDocument.authority], ["Issue date", demoDocument.issueDate], ["Expiry date", demoDocument.expiryDate]].map(([label, value]) => <div key={label}><div className="text-[10px] text-muted-foreground">{label}</div><div className="mt-1 text-xs font-semibold">{value}</div></div>)}</div><div className="mt-5"><div className="text-[10px] text-muted-foreground">Address</div><div className="mt-1 text-xs font-semibold">{demoDocument.address}</div></div></div><div><h3 className="text-xs font-bold uppercase tracking-[0.14em] text-cyan">Recommendation</h3><div className="mt-4 rounded-md border border-warning/25 bg-warning/5 p-4"><div className="text-sm font-semibold">Continue with manual review</div><p className="mt-2 text-xs leading-relaxed text-muted-foreground">{demoDocument.explanation} Compare suspicious regions with an official source before making a decision.</p></div><div className="mt-5"><h3 className="text-xs font-bold uppercase tracking-[0.14em] text-cyan">Checks performed</h3><div className="mt-3 flex flex-wrap gap-2">{demoChecks.map((check) => <StatusPill key={check.name} value={check.state} />)}</div></div></div></div><div className="border-t border-border px-6 py-5 sm:px-8"><h3 className="text-xs font-bold uppercase tracking-[0.14em] text-cyan">Detected anomalies</h3><div className="mt-3 flex items-start gap-3 rounded-md bg-danger/5 p-4"><ShieldAlert size={17} className="mt-0.5 shrink-0 text-danger" /><div><div className="text-xs font-semibold">Region 01 · Formatting anomaly</div><p className="mt-1 text-xs leading-relaxed text-muted-foreground">Baseline and font-weight variation detected around the document number. Manual inspection is recommended.</p></div></div></div><div className="border-t border-border bg-secondary/35 px-6 py-5 text-[10px] leading-relaxed text-muted-foreground sm:px-8">AI screening provides a risk assessment and does not constitute official identity verification. This report contains fictional sample data for demonstration only.</div></Surface><Link to="/history" className="mt-5 inline-flex items-center gap-2 text-xs font-semibold text-primary"><ArrowLeft size={14} />Back to verification history</Link></div></div>;
}
