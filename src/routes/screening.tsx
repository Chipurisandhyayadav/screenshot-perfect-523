import { createFileRoute, Link } from "@tanstack/react-router";
import { CheckCircle2, FileUp, Info, LockKeyhole, UploadCloud } from "lucide-react";
import { useRef, useState } from "react";
import { Button } from "@/components/ui/button";
import { PageIntro, Surface } from "@/components/veridoc-shell";
import { demoDocument } from "@/lib/veridoc-data";
export const Route = createFileRoute("/screening")({ head: () => ({ meta: [{ title: "Screen document — VeriDoc AI" }, { name: "description", content: "Upload a fictional identity document for the VeriDoc AI demonstration workflow." }, { property: "og:title", content: "Screen document — VeriDoc AI" }, { property: "og:description", content: "Upload a fictional identity document for the VeriDoc AI demonstration workflow." }] }), component: Screening });
function Screening() { const inputRef = useRef<HTMLInputElement>(null); const [fileName, setFileName] = useState(""); const [dragging, setDragging] = useState(false); const [analyzing, setAnalyzing] = useState(false); const acceptFile = (file?: File) => { if (file) setFileName(file.name); }; const analyze = () => { setAnalyzing(true); window.setTimeout(() => setAnalyzing(false), 850); }; return <div className="app-grid min-h-[calc(100vh-70px)] px-5 py-8 sm:px-8 lg:px-10"><div className="mx-auto max-w-[1180px]"><PageIntro eyebrow="Document screening" title="Start a new verification" description="Upload a document to begin the structured screening pipeline. This demonstration uses static sample findings until a backend is connected." /><div className="mb-6 flex gap-3 rounded-md border border-primary/20 bg-primary/5 p-4 text-xs text-ink-soft"><Info size={17} className="mt-0.5 shrink-0 text-primary" /><p><strong className="text-foreground">Demo Mode.</strong> No document is sent for AI analysis in this prototype. Choose a file to preview the workflow, then open the prepared sample result.</p></div><div className="grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><Surface className="p-5 sm:p-7"><div onDragEnter={() => setDragging(true)} onDragLeave={() => setDragging(false)} onDragOver={(event) => event.preventDefault()} onDrop={(event) => { event.preventDefault(); setDragging(false); acceptFile(event.dataTransfer.files[0]); }} className={`flex min-h-[330px] flex-col items-center justify-center rounded-md border border-dashed px-6 text-center transition-colors ${dragging ? "border-cyan bg-accent/40" : "border-border bg-secondary/25"}`}><div className="mb-5 flex h-14 w-14 items-center justify-center rounded-full bg-primary/10 text-primary"><UploadCloud size={26} /></div><h2 className="text-base font-semibold">Drop your document here</h2><p className="mt-2 max-w-sm text-xs leading-relaxed text-muted-foreground">Upload a clear image or PDF of the identity document. Maximum file size 10 MB.</p><input ref={inputRef} type="file" accept=".png,.jpg,.jpeg,.pdf" className="hidden" onChange={(event) => acceptFile(event.target.files?.[0])} /><Button variant="outline" className="mt-6" onClick={() => inputRef.current?.click()}>Browse files</Button><div className="mt-5 flex items-center gap-2 text-[10px] text-muted-foreground"><span className="rounded border border-border bg-card px-2 py-1">PNG</span><span className="rounded border border-border bg-card px-2 py-1">JPG / JPEG</span><span className="rounded border border-border bg-card px-2 py-1">PDF</span></div></div>{fileName && <div className="mt-4 flex items-center justify-between rounded-md border border-success/25 bg-success/5 px-4 py-3"><div className="flex items-center gap-3"><FileUp size={18} className="text-success" /><div><div className="text-xs font-semibold">{fileName}</div><div className="mt-1 text-[10px] text-muted-foreground">Ready for demo screening</div></div></div><CheckCircle2 size={17} className="text-success" /></div>}<div className="mt-6 flex flex-col justify-between gap-3 border-t border-border pt-5 sm:flex-row sm:items-center"><div className="flex items-center gap-2 text-[11px] text-muted-foreground"><LockKeyhole size={14} /> Files stay in this demo session</div><Link to="/analysis"><Button onClick={analyze} disabled={analyzing}>{analyzing ? "Preparing sample result..." : "Analyze in demo mode"}</Button></Link></div></Surface><div className="space-y-5"><Surface className="p-5"><div className="text-[10px] font-bold uppercase tracking-[0.16em] text-cyan">Screening pipeline</div><div className="mt-5 space-y-4">{["OCR & field extraction", "Document quality analysis", "Consistency analysis", "Risk scoring", "Human review"].map((step, i) => <div key={step} className="flex items-center gap-3"><div className={`flex h-7 w-7 items-center justify-center rounded-full text-[10px] font-semibold ${i === 0 ? "bg-primary text-primary-foreground" : "bg-secondary text-muted-foreground"}`}>{i + 1}</div><span className="text-xs font-medium">{step}</span></div>)}</div></Surface><Surface className="p-5"><div className="flex items-center gap-2 text-sm font-semibold"><Info size={16} className="text-warning-foreground" />Before you continue</div><ul className="mt-4 space-y-3 text-xs leading-relaxed text-muted-foreground"><li>Use fictional records only in this demonstration.</li><li>Make sure the document is readable and uncropped.</li><li>Every result must be reviewed by an authorized person.</li></ul></Surface><div className="rounded-md bg-navy px-5 py-4 text-primary-foreground"><div className="text-xs font-semibold">Important limitation</div><p className="mt-2 text-[11px] leading-relaxed text-primary-foreground/65">AI screening provides a risk assessment and does not constitute official identity verification.</p></div></div></div><div className="mt-8 text-center text-[10px] uppercase tracking-[0.15em] text-muted-foreground">Sample identity: {demoDocument.id} · fictional data only</div></div></div>; }
