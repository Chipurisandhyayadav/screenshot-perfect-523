import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, LockKeyhole, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
export const Route = createFileRoute("/login")({ head: () => ({ meta: [{ title: "Sign in — VeriDoc AI" }, { name: "description", content: "Sign in to the VeriDoc AI identity screening workspace." }, { property: "og:title", content: "Sign in — VeriDoc AI" }, { property: "og:description", content: "Sign in to the VeriDoc AI identity screening workspace." }] }), component: Login });
function Login() { const [demo, setDemo] = useState(false); return <div className="flex min-h-screen items-center justify-center bg-navy px-5 py-10"><div className="absolute inset-0 opacity-20" style={{ backgroundImage: "linear-gradient(to right, color-mix(in oklch, var(--cyan) 20%, transparent) 1px, transparent 1px), linear-gradient(to bottom, color-mix(in oklch, var(--cyan) 20%, transparent) 1px, transparent 1px)", backgroundSize: "32px 32px" }} /><div className="relative grid w-full max-w-[920px] overflow-hidden rounded-lg border border-sidebar-border bg-card shadow-2xl lg:grid-cols-[.9fr_1.1fr]"><div className="hidden flex-col justify-between bg-sidebar p-9 text-sidebar-foreground lg:flex"><div><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-lg bg-sidebar-primary text-sidebar-primary-foreground"><ShieldCheck size={23} /></div><div className="text-base font-semibold">VeriDoc <span className="text-sidebar-primary">AI</span></div></div><div className="mt-24"><div className="text-[10px] font-bold uppercase tracking-[0.18em] text-sidebar-primary">Authorized access only</div><h1 className="mt-4 text-3xl font-semibold leading-tight">Make every document decision with more signal.</h1><p className="mt-4 text-sm leading-relaxed text-sidebar-foreground/60">A structured screening workspace for identifying risk signals and routing documents to human review.</p></div></div><div className="text-[10px] leading-relaxed text-sidebar-foreground/45">AI screening provides a risk assessment and does not constitute official identity verification.</div></div><div className="p-7 sm:p-10"><div className="mb-10 lg:hidden"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-lg bg-primary text-primary-foreground"><ShieldCheck size={23} /></div><div className="text-base font-semibold">VeriDoc <span className="text-cyan">AI</span></div></div></div><div className="mx-auto max-w-sm"><div className="text-[10px] font-bold uppercase tracking-[0.18em] text-cyan">Reviewer portal</div><h2 className="mt-3 text-2xl font-semibold">Welcome back</h2><p className="mt-2 text-sm text-muted-foreground">Sign in to continue to your screening workspace.</p><form className="mt-8 space-y-4" onSubmit={(event) => { event.preventDefault(); setDemo(false); }}><label className="block text-xs font-medium">Work email<input type="email" defaultValue="reviewer@veridoc.demo" className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm outline-none focus:ring-1 focus:ring-ring" /></label><label className="block text-xs font-medium">Password<input type="password" defaultValue="demo-password" className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm outline-none focus:ring-1 focus:ring-ring" /></label><Link to="/"><Button className="mt-3 w-full" type="button">Sign in <ArrowRight size={15} /></Button></Link></form><div className="my-7 flex items-center gap-3 text-[10px] uppercase tracking-[0.14em] text-muted-foreground"><span className="h-px flex-1 bg-border" />or<span className="h-px flex-1 bg-border" /></div><Link to="/"><Button variant="outline" className="w-full" onClick={() => setDemo(true)}><ShieldCheck size={15} />Continue with demo access</Button></Link>{demo && <div className="mt-4 rounded-md border border-success/25 bg-success/5 p-3 text-xs text-success-foreground">Demo access enabled. Welcome to the sample workspace.</div>}<div className="mt-7 flex items-center justify-center gap-2 text-[10px] text-muted-foreground"><LockKeyhole size={13} /> Protected reviewer workspace · DEMO MODE</div></div></div></div></div>; }
